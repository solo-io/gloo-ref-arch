steps:
  - apply:
      manifest:
        path: sample-app.yaml
  - apply:
      manifest:
        path: vs.yaml
    values:
      RenderAsYaml: true
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      statusCode: 404