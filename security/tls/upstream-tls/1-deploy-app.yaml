title: Deploy the example application
steps:
  - apply:
      manifest:
        path: sample-app.yaml
  - apply:
      manifest:
        path: vs.yaml
    docsValues:
      RenderAsYaml: "true"
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      statusCode: 404