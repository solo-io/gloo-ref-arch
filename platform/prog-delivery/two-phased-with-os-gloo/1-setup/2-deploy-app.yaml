steps:
  - apply:
      manifest:
        path: echo.yaml
  - apply:
      manifest:
        path: upstream.yaml
  - apply:
      manifest:
        path: vs.yaml
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      statusCode: 200
      path: /
      responseBody: "version:v1"