steps:
  - apply:
      manifest:
        path: upstream-canary.yaml
  - apply:
      manifest:
        path: vs-2.yaml
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      path: /
      statusCode: 200
      responseBody: "version:v1"
