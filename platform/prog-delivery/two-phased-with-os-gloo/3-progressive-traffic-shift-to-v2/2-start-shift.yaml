steps:
  - apply:
      manifest:
        path: vs-2.yaml
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      path: /
      statusCode: 200
      responseBody: "version:v1"
  - curl:
      service:
        name: gateway-proxy
        namespace: gloo-system
      path: /
      statusCode: 200
      responseBody: "version:v2"
